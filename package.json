{
  "name": "ramenpad",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["frontend", "backend/ramenpad"],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace @ramenpad/backend",
    "build": "npm run build --workspace frontend && npm run build --workspace @ramenpad/backend",
    "lint": "npm run lint --workspace frontend",
    "test": "npm run test --workspace frontend && npm run test --workspace @ramenpad/backend",
    "contracts:build": "forge build",
    "contracts:test": "forge test -vvv"
  },
  "engines": { "node": "20.x" }
}
